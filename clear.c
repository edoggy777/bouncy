#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main() {
   for (int from = 0; frame < 5; frame++) {
      system("clear");
      printf("Frame %d\n", frame);
      usleep(500000);
   }

   return 0;
}
